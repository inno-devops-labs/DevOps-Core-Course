services:
  app:
    build:
      context: ./app_python
      dockerfile: Dockerfile
    image: darriyan0/app_python:lab12
    container_name: python-app
    ports:
      - "8000:5000"
    environment:
      VISITS_FILE: /data/visits
    volumes:
      - ./data:/data