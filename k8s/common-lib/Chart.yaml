apiVersion: v2
name: common-lib
description: Shared Helm template library for DevOps Core Course applications
# type: library means this chart cannot be installed directly —
# it only provides named templates for use by application charts as a dependency
type: library
version: 0.1.0
