{{/*
common-lib — shared template library for DevOps Core Course

Available templates:
  common.name             — chart name, trimmed to 63 chars
  common.fullname         — release-name + chart-name, trimmed to 63 chars
  common.chart            — "chart-version" string for helm.sh/chart label
  common.labels           — full recommended label set (can change on upgrade)
  common.selectorLabels   — stable selector labels (MUST NOT change after install)
*/}}
