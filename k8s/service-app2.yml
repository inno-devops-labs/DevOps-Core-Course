apiVersion: v1
kind: Service
metadata:
  name: devops-app2-service
  labels:
    app: devops-app2
spec:
  type: NodePort
  selector:
    app: devops-app2
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30081
