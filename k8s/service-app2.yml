apiVersion: v1
kind: Service
metadata:
  name: devops-info-app2-service
  labels:
    app: devops-info-app2
    app.kubernetes.io/name: devops-info
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app: devops-info-app2
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
