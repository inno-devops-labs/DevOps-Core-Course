{{- if and .Values.deployment.enabled (not .Values.rollout.enabled) (not .Values.statefulset.enabled) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "devops-app-py.fullname" . }}
  labels:
    {{- include "devops-app-py.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: {{ .Values.deployment.revisionHistoryLimit }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: {{ .Values.deployment.strategy.maxSurge }}
      maxUnavailable: {{ .Values.deployment.strategy.maxUnavailable }}
  selector:
    matchLabels:
      {{- include "devops-app-py.selectorLabels" . | nindent 6 }}
  template:
    {{- include "devops-app-py.podTemplate" . | nindent 4 }}
{{- end }}
