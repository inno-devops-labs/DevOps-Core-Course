{{- if and .Values.rollout.enabled (eq .Values.rollout.strategy "blueGreen") }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pythonapp.fullname" . }}-preview
  labels:
    {{- include "pythonapp.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "pythonapp.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
{{- end }}
