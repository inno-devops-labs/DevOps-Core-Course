apiVersion: v1
kind: Service
metadata:
  name: devops-info-service-alt
  namespace: devops-lab9
  labels:
    app.kubernetes.io/name: devops-info-service-alt
    app.kubernetes.io/part-of: devops-core-course
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: devops-info-service-alt
    app.kubernetes.io/component: web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
