apiVersion: v1
kind: Service
metadata:
  name: devops-info-service-active
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: devops-info-service
    app.kubernetes.io/instance: devops-info-service
  ports:
  - name: http
    port: 80
    targetPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: devops-info-service-preview
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: devops-info-service
    app.kubernetes.io/instance: devops-info-service
  ports:
  - name: http
    port: 80
    targetPort: 5000