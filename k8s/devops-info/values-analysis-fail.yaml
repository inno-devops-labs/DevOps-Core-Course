statefulset:
  enabled: false

rollout:
  enabled: true
  analysis:
    path: /missing
    successCondition: result == "ready"

env:
  SERVICE_DESCRIPTION: "Intentional failed-analysis deployment for Argo Rollouts"
