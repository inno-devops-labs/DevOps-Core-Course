DevOps Info Service is deployed.

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Service type: {{ .Values.service.type }}

{{- if eq .Values.service.type "NodePort" }}
Get access URL:
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "devops-info.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT/health
{{- else }}
Check service external status:
  kubectl get svc -n {{ .Release.Namespace }} {{ include "devops-info.fullname" . }}
{{- end }}
