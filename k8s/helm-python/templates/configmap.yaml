apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "helm-python.fullname" . }}-config
  labels:
    {{- include "helm-python.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Files.Get "files/config.json" | nindent 4 }}