apiVersion: v1
kind: Service
metadata:
  name: devops-info-python-service
  labels:
    app: devops-info-python
spec:
  type: NodePort
  selector:
    app: devops-info-python
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
