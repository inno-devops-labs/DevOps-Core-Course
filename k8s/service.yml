apiVersion: v1
kind: Service
metadata:
  name: devops-python-service
  labels:
    app: devops-python
spec:
  type: NodePort
  selector:
    app: devops-python
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
