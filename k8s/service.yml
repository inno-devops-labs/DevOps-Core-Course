apiVersion: v1
kind: Service
metadata:
  name: devops-info-service
  labels:
    app: devops-info
spec:
  type: NodePort
  selector:
    app: devops-info
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
