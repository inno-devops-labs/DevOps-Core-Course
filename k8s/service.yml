apiVersion: v1
kind: Service
metadata:
  name: devops-info-service
  namespace: lab9
  labels:
    app: devops-info-service
    lab: "9"
spec:
  type: NodePort
  selector:
    app: devops-info-service
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
      protocol: TCP
