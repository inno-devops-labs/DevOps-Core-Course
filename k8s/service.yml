apiVersion: v1
kind: Service
metadata:
  name: devops-info-service
  namespace: devops-lab09
  labels:
    app.kubernetes.io/name: devops-info
    app.kubernetes.io/instance: primary
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: devops-lab09
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: devops-info
    app.kubernetes.io/instance: primary
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
