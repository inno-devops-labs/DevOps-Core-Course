apiVersion: v1
kind: Service
metadata:
  name: testiks
  labels:
    app: testiks
spec:
  type: NodePort
  selector:
    app: testiks
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080