apiVersion: v1
kind: Service
metadata:
  name: devops-info-service
  labels:
    app: devops-info
    app.kubernetes.io/name: devops-info-service
spec:
  type: NodePort
  selector:
    app: devops-info
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
