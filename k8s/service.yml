apiVersion: v1
kind: Service
metadata:
  name: devops-python-app-service
  labels:
    app: devops-python-app
spec:
  type: NodePort
  selector:
    app: devops-python-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080