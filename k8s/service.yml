apiVersion: v1
kind: Service
metadata:
  name: app-python-service
  namespace: devops-lab9
spec:
  type: NodePort
  selector:
    app: app-python
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
