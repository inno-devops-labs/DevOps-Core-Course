apiVersion: v1
kind: Service
metadata:
  name: pythonapp-service
  labels:
    app: pythonapp
spec:
  type: NodePort
  selector:
    app: pythonapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
