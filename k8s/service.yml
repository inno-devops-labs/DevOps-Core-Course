apiVersion: v1
kind: Service
metadata:
  name: devops-python
  labels:
    app: devops-python
    lab: "09"
spec:
  type: NodePort
  selector:
    app: devops-python
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
