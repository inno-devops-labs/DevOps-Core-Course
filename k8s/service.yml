apiVersion: v1
kind: Service
metadata:
  name: devops-info-service
  labels:
    app.kubernetes.io/name: devops-info-service
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: devops-info-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
