apiVersion: v1
kind: Service
metadata:
  name: devops-lab2-service
  labels:
    app: devops-lab2
spec:
  type: NodePort
  selector:
    app: devops-lab2
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
