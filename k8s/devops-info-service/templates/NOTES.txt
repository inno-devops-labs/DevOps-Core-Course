Release "{{ .Release.Name }}" installed.

The DevOps Info Service is exposed by Service "{{ include "devops-info-service.fullname" . }}".

{{- if eq .Values.service.type "NodePort" }}

NodePort access (example):
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "devops-info-service.fullname" . }})
  export NODE_IP=$(kubectl get nodes -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "http://${NODE_IP}:${NODE_PORT}"
{{- else if eq .Values.service.type "LoadBalancer" }}

LoadBalancer: wait for EXTERNAL-IP, then:
  kubectl get svc {{ include "devops-info-service.fullname" . }} -n {{ .Release.Namespace }} -w
{{- else }}

Port-forward for local testing:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "devops-info-service.fullname" . }} 8080:{{ .Values.service.port }}
  curl -s http://127.0.0.1:8080/health
{{- end }}

Default app port in container: {{ .Values.containerPort }}.
