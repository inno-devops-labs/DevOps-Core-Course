apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "devops-service.name" . }}-config
  labels:
    {{- include "devops-service.labels" . | nindent 4 }}
data:
  config.json: |-
{{ .Files.Get "files/config.json" | indent 4 }}