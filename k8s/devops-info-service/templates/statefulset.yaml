{{- if .Values.statefulset.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "devops-info-service.fullname" . }}
  labels:
    {{- include "devops-info-service.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "devops-info-service.fullname" . }}-headless
  replicas: {{ .Values.replicaCount }}
  podManagementPolicy: {{ .Values.statefulset.podManagementPolicy }}
  revisionHistoryLimit: {{ .Values.statefulset.revisionHistoryLimit }}

  updateStrategy:
    type: {{ .Values.statefulset.updateStrategy.type }}
    {{- if eq .Values.statefulset.updateStrategy.type "RollingUpdate" }}
    rollingUpdate:
      partition: {{ .Values.statefulset.updateStrategy.partition }}
    {{- end }}

  selector:
    matchLabels:
      {{- include "devops-info-service.selectorLabels" . | nindent 6 }}

  template:
    metadata:
      labels:
        {{- include "devops-info-service.labels" . | nindent 8 }}
      annotations:
        {{- if .Values.vault.enabled }}
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: {{ .Values.vault.role | quote }}
        vault.hashicorp.com/agent-inject-secret-config.txt: {{ .Values.vault.secretPath | quote }}
        vault.hashicorp.com/agent-inject-template-config.txt: |
            {{`{{- with secret "secret/data/devops-info-service/config" -}}
            username={{ .Data.data.username }}
            password={{ .Data.data.password }}
            {{- end }}`}}
        {{- end }}
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}

    spec:
      serviceAccountName: devops-info-service
      securityContext:
        fsGroup: 10001

      {{- if .Values.initContainers.enabled }}
      initContainers:
        {{- if .Values.initContainers.waitForService.enabled }}
        - name: wait-for-service
          image: {{ .Values.initContainers.image | quote }}
          command:
            - sh
            - -c
            - |
              echo "Waiting for service: {{ .Values.initContainers.waitForService.host }}"
              until nslookup {{ .Values.initContainers.waitForService.host }}; do
                echo "Service {{ .Values.initContainers.waitForService.host }} is not ready yet"
                sleep {{ .Values.initContainers.waitForService.sleepSeconds }}
              done
              echo "Service {{ .Values.initContainers.waitForService.host }} is resolvable"
        {{- end }}

        {{- if .Values.initContainers.download.enabled }}
        - name: init-download
          image: {{ .Values.initContainers.image | quote }}
          command:
            - sh
            - -c
            - |
              echo "Downloading file from {{ .Values.initContainers.download.url }}"
              mkdir -p /work-dir
              wget -O /work-dir/{{ .Values.initContainers.download.filename }} {{ .Values.initContainers.download.url | quote }}
              echo "Downloaded file:"
              ls -la /work-dir
              echo "File preview:"
              head -n 5 /work-dir/{{ .Values.initContainers.download.filename }} || true
          volumeMounts:
            - name: init-workdir
              mountPath: /work-dir
        {{- end }}
      {{- end }}

      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          ports:
            - name: http
              containerPort: {{ .Values.containerPort }}

          env:
            - name: HOST
              value: {{ .Values.env.HOST | quote }}
            - name: PORT
              value: {{ .Values.env.PORT | quote }}
            - name: DEBUG
              value: {{ .Values.env.DEBUG | quote }}
            - name: RELEASE_VERSION
              value: {{ .Values.env.RELEASE_VERSION | quote }}

          envFrom:
            - configMapRef:
                name: {{ include "devops-info-service.fullname" . }}-env
            - secretRef:
                name: {{ include "devops-info-service.fullname" . }}-secret

          volumeMounts:
            - name: config-volume
              mountPath: /config
              readOnly: true
            {{- if .Values.persistence.enabled }}
            - name: data
              mountPath: /data
            {{- end }}
            {{- if .Values.initContainers.enabled }}
            - name: init-workdir
              mountPath: {{ .Values.initContainers.mountPath }}
              readOnly: true
            {{- end }}

          resources:
            {{- toYaml .Values.resources | nindent 12 }}

          readinessProbe:
            httpGet:
              path: {{ .Values.readinessProbe.path }}
              port: {{ .Values.containerPort }}
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}

          livenessProbe:
            httpGet:
              path: {{ .Values.livenessProbe.path }}
              port: {{ .Values.containerPort }}
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.livenessProbe.periodSeconds }}

      volumes:
        - name: config-volume
          configMap:
            name: {{ include "devops-info-service.fullname" . }}-config
        {{- if .Values.initContainers.enabled }}
        - name: init-workdir
          emptyDir: {}
        {{- end }}

  {{- if .Values.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          {{- include "devops-info-service.labels" . | nindent 10 }}
      spec:
        accessModes:
          - {{ .Values.persistence.accessMode | default "ReadWriteOnce" | quote }}
        resources:
          requests:
            storage: {{ .Values.persistence.size }}
        {{- if .Values.persistence.storageClass }}
        storageClassName: {{ .Values.persistence.storageClass | quote }}
        {{- end }}
  {{- end }}
{{- end }}