apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-service.name" . }}-preview
spec:
  selector:
    {{- include "devops-service.labels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
