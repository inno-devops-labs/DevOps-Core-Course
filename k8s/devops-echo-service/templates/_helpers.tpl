{{/*
Re-export common-lib templates so they resolve in this chart's context.
The common-lib dependency provides: common.name, common.fullname, common.chart,
common.labels, common.selectorLabels.
*/}}
