---
# Bonus app (Go devops-info-service-go) for multi-app deployment
app_name: devops-go
docker_image: "{{ dockerhub_username }}/devops-info-service-go"
docker_tag: latest
app_port: 8001
app_internal_port: 8080
compose_project_dir: "/opt/{{ app_name }}"
