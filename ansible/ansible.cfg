[defaults]
inventory = inventory/hosts.ini
roles_path = roles
host_key_checking = False
remote_user = ubuntu
retry_files_enabled = False
# vault_password_file should be provided via --vault-password-file or ANSIBLE_VAULT_PASSWORD_FILE env var

[privilege_escalation]
become = True
become_method = sudo
become_user = root
