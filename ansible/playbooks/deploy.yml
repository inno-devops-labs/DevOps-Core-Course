---
- name: Deploy application
  hosts: webservers
  become: true

  pre_tasks:
    - name: Debug - show dockerhub_username
      ansible.builtin.debug:
        var: dockerhub_username

  roles:
    - web_app
