---
- name: Deploy application
  hosts: webservers
  become: true
  vars_files:
    - "../group_vars/all.yml"

  roles:
    - role: web_app
      tags:
        - app_deploy
        - compose
        - web_app_wipe
