---
# Main playbook: provision + deploy
- name: Provision web servers
  hosts: webservers
  become: true
  roles:
    - common
    - docker

- name: Deploy application
  hosts: webservers
  become: true
  roles:
    - app_deploy
