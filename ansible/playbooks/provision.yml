---
- name: Provision web servers
  hosts: webservers
  become: yes

  roles:
    - { role: common, tags: [common, packages, users] }
    - { role: docker, tags: [docker, docker_install, docker_config] }
