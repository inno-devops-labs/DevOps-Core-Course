---
- name: Deploy Python Application
  hosts: webservers
  become: yes

  vars_files:
    - ../group_vars/all.yml
    - ../vars/app_python.yml

  roles:
    - role: web_app
      tags:
        - app_deploy
        - compose
        - web_app_wipe
