---
- name: Deploy observability stack
  hosts: webservers
  become: yes

  vars_files:
    - ../group_vars/all.yml

  roles:
    - role: monitoring
      tags:
        - monitoring
        - compose
