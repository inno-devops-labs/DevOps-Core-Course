# For self-hosted runner: deploy runs on this machine (localhost).
# Use: ansible-playbook ... -i inventory/hosts.local.ini
[webservers]
localhost ansible_connection=local

[webservers:vars]
ansible_python_interpreter=/usr/bin/python3
