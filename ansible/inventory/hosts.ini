# Replace YOUR_VM_IP with your VM's public IP (Pulumi: pulumi stack output public_ip | Terraform: terraform output vm_public_ip)
# Replace ubuntu with your SSH user if different
[webservers]
web1 ansible_host=89.169.129.155 ansible_user=ubuntu

[webservers:vars]
ansible_python_interpreter=/usr/bin/python3
