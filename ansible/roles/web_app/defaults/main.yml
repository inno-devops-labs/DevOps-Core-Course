
app_name: devops-app
docker_image: cacucoh/testiks
docker_tag: "1.0"
app_port: 8000
app_internal_port: 5000
restart_policy: unless-stopped

docker_compose_version: "3.8"
compose_project_dir: "/opt/{{ app_name }}"

app_env:
  APP_ENV: production
  LOG_LEVEL: INFO


web_app_wipe: false
