services:
  {{ app_name }}:
    image: "{{ docker_image }}:{{ web_app_docker_image_tag }}"
    ports:
      - "{{ web_app_app_port }}:{{ web_app_container_port }}"
    environment: {{ web_app_env | to_nice_yaml(indent=6) | trim }}
    restart: "{{ web_app_restart_policy }}"
