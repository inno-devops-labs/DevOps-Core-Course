---
- name: Restart application via docker compose CLI
  ansible.builtin.command:
    cmd: >-
      docker compose
      -p {{ web_app_compose_project_name }}
      -f {{ web_app_compose_project_dir }}/docker-compose.yml
      restart {{ web_app_name }}
  changed_when: true
  listen: restart app container
