---
# Main task file for monitoring role

- name: Include setup tasks
  ansible.builtin.include_tasks: setup.yml
  tags:
    - setup

- name: Include deploy tasks
  ansible.builtin.include_tasks: deploy.yml
  tags:
    - deploy
