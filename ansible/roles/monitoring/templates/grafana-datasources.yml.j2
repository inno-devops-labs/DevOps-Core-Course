apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:{{ prometheus_port }}
    isDefault: true
    editable: false

  - name: Loki
    type: loki
    access: proxy
    url: http://loki:{{ loki_port }}
    editable: false
