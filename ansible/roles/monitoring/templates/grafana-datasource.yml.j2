apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:{{ monitoring_loki_port }}
    isDefault: false
    editable: true
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:{{ monitoring_prometheus_port }}
    isDefault: true
    editable: true
