# Grafana datasource provisioning - Loki (Ansible Lab 7)
apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:{{ loki_port }}
    isDefault: true
    editable: false
