---
common_packages:
  - python3-pip
  - curl
  - git
  - vim
  - htop
  - unzip
  - ca-certificates
  - gnupg
  - lsb-release
common_timezone: "UTC"
# Optional: list of users to create, e.g. [{ name: deploy, groups: [docker] }]
common_users: []
