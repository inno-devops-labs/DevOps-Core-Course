---
# List of packages to install on all servers
common_packages:
  - python3-pip
  - curl
  - git
  - vim
  - htop
  - unzip
  - ca-certificates
  - gnupg
  - software-properties-common

# Timezone (optional)
common_timezone: UTC
