---
common_timezone: UTC
common_packages:
  - python3-pip
  - curl
  - git
  - vim
  - htop
  - ca-certificates
  - gnupg
  - lsb-release

# Deployment user/group (created by users block)
common_deploy_user: deploy
common_deploy_group: deploy
