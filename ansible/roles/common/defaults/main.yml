---
common_apt_cache_valid_time: 3600

common_packages:
  - python3-pip
  - curl
  - git
  - vim
  - htop
  - ca-certificates
  - gnupg
  - lsb-release
  - unzip

common_timezone: "UTC"
common_manage_user: true
common_user_name: "{{ ansible_user | default('ubuntu') }}"
