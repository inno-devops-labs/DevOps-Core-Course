---
- name: Install common packages with error handling
  block:
    - name: Update apt cache
      apt:
        update_cache: true
        cache_valid_time: 3600

    - name: Install common packages
      apt:
        name: "{{ common_packages }}"
        state: present
  rescue:
    - name: "Rescue: Fix apt cache issues"
      ansible.builtin.apt:
        update_cache: true
      become: true
      tags:
        - packages
  always:
    - name: Log package installation completion
      ansible.builtin.file:
        path: /tmp/common_packages_done
        state: touch
      tags:
        - packages
  become: true
  tags:
    - packages
    - common

# Example user management block (add your user tasks here if needed)
#- name: User management tasks
#  block:
#    - name: Create app user
#      user:
#        name: "{{ app_user }}"
#        state: present
#  become: true
#  tags:
#    - users
#    - common

- name: Set timezone
  timezone:
    name: "{{ timezone }}"
  tags:
    - common
