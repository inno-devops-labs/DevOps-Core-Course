---
# default variables for app deployment

app_name: devops-app
docker_image_tag: latest
docker_image: "{{ dockerhub_username | default('') }}/{{ app_name }}"
app_container_name: "{{ app_name }}"
app_port: 5000
app_restart_policy: unless-stopped
app_env: {}
