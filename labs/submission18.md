# Lab 18 Submission

The Lab 18 reproducible-build implementation and report are kept in [`../nix/README.md`](../nix/README.md).

This compatibility file exists because the lab handout asks for `labs/submission18.md`, while this repository keeps each major tool area in its own module directory.
