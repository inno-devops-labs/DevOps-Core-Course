[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# Keep it simple for a course repo: basic correctness + style.
select = ["E", "F", "I", "B", "UP"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["app"]
