version: "3.9"

services:
  app:
    build: .
    container_name: devops-info-app
    ports:
      - "8080:8080"
    environment:
      HOST: "0.0.0.0"
      PORT: "8080"
      DATA_DIR: "/data"
      VISITS_FILE: "/data/visits"
    volumes:
      - ./data:/data
