# Go Language Justification

Go was chosen for this bonus task because it is widely used in DevOps and cloud-native environments for building fast, lightweight, and reliable services. The standard `net/http` library allows creating a fully functional HTTP API without external dependencies, which simplifies deployment and maintenance. Additionally, Go compiles into a single binary, making it convenient for containerization and infrastructure tooling.
