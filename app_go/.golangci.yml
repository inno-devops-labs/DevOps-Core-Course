linters-settings:
  errcheck:
    check-type-assertions: false
    check-blank: false

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
