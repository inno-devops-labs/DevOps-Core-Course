version: '3.8'

services:
  devops-info-service:
    build: .
    ports:
      - "8080:5000"
    volumes:
      - ./data:/data
    environment:
      - HOST=0.0.0.0
      - PORT=5000
      - DEBUG=True
      - VISITS_FILE=/data/visits
