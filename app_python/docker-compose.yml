services:
  app:
    build: .
    image: devops-app:lab12
    container_name: devops-app-lab12
    network_mode: host
    environment:
      HOST: 0.0.0.0
      PORT: 5000
      DEBUG: "false"
      VISITS_FILE: /data/visits
    volumes:
      - ./data:/data
    restart: unless-stopped
