services:
  devops-info-python:
    build: .
    container_name: devops-info-python
    ports:
      - "8080:8080"
    environment:
      HOST: "0.0.0.0"
      PORT: "8080"
      DEBUG: "False"
    volumes:
      - ./data:/data
