services:
  devops-info:
    image: devops-info-service-python:lab12
    container_name: devops-info-python
    ports:
      - "5001:5000"
    environment:
      HOST: "0.0.0.0"
      PORT: "5000"
      DEBUG: "false"
      VISITS_FILE: "/data/visits"
    volumes:
      - ./data:/data
