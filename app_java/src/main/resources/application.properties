server.address=${HOST:localhost}
server.port=${PORT:5000}
debug=${DEBUG:false}

spring.application.name=devops-info-service
spring.application.version=1.0.0
spring.application.description=DevOps course info service
