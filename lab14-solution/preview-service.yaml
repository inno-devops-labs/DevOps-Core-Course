apiVersion: v1
kind: Service
metadata:
  name: lab14-rollouts-preview
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: lab14-rollouts
