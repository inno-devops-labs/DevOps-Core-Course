# Lab 15 Solution

StatefulSet with per-pod persistent storage for visits counter.

## Local Testing

```bash
docker-compose up --build
```

Access http://localhost:8000/visits for per-pod visit counts.