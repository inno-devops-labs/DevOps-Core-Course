workload:
  kind: StatefulSet

replicaCount: 3

image:
  repository: devops-info-service
  tag: "lab15"
  pullPolicy: IfNotPresent

env:
  releaseVersion: "statefulset-ondelete-v1"

statefulset:
  podManagementPolicy: OrderedReady
  updateStrategy:
    type: OnDelete
    rollingUpdate: {}

persistence:
  enabled: true
  size: 100Mi

hooks:
  enabled: false
