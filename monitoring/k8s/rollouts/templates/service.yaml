apiVersion: v1
kind: Service
metadata:
  name: devops-info-service
spec:
  type: ClusterIP
  selector:
    app: devops-info
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: http
