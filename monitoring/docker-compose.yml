services:
  loki:
    image: grafana/loki:3.0.0
    ports:
      - "3100:3100"
    networks:
      - default
      - logging
    volumes:
      - ./loki/config.yml:/etc/loki/config.yml
      - loki-data:/loki
    command: -config.file=/etc/loki/config.yml
    healthcheck:
      start_period: 30s
      timeout: 10s
      interval: 10s
      retries: 10
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://loki:3100/ready"]
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M

  promtail:
    image: grafana/promtail:3.0.0
    volumes:
      - ./promtail/config.yml:/etc/promtail/config.yml
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: -config.file=/etc/promtail/config.yml
    networks:
      - logging
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M

  grafana:
    image: grafana/grafana:12.3.1
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=false
      - GF_AUTH_BASIC_ENABLED=true
      - GF_AUTH_LDAP_ENABLED=true
    env_file:
      - ./grafana.env
    networks:
      - default
      - logging
    healthcheck:
      start_period: 30s
      timeout: 10s
      interval: 10s
      retries: 10
      test: ["CMD", "curl", "--silent", "http://grafana:3000/api/health"]
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M

  prometheus:
    image: prom/prometheus:v3.8.0
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/config.yml:/etc/prometheus/config.yml
      - prometheus-data:/data
    networks:
      - logging
    command: --config.file=/etc/prometheus/config.yml
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M

  infoservice-python:
    image: ub3rch/infoservice:python-latest
    ports:
      - "8000:8000"
    volumes:
      - python-app-data:/data
    networks:
      - default
      - logging
    labels:
      logging: "promtail"
      app: "devops-python"
    healthcheck:
      start_period: 30s
      timeout: 10s
      interval: 10s
      retries: 10
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://infoservice-python:8000/health"]
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M

  infoservice-go:
    image: ub3rch/infoservice:go-latest
    ports:
      - "8001:8000"
    networks:
      - default
      - logging
    labels:
      logging: "promtail"
      app: "devops-go"
    healthcheck:
      start_period: 30s
      timeout: 10s
      interval: 10s
      retries: 10
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://infoservice-go:8000/health"]
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M

networks:
  logging:
    driver: bridge

volumes:
  prometheus-data:
  grafana-data:
  loki-data:
  python-app-data:
