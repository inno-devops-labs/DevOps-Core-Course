apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: lab16-app-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: lab16-app
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
