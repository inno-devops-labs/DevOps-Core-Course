services:
  app:
    build: .
    ports:
      - "5001:5000"
    environment:
      HOST: 0.0.0.0
      PORT: 5000
      DEBUG: "False"
      VISITS_FILE: /data/visits
    volumes:
      - ./data:/data
    restart: unless-stopped
